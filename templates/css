/* styles.css */
.icon-button {
    background: none;
    border: none;
    cursor: pointer;
}

.initials-circle {
    border-radius: 50%;
    width: 40px;
    height: 40px;
    text-align: center;
    line-height: 40px; /* Vertically center the text */
    cursor: pointer;
}

/* Add your own colors, sizes, hover effects, etc. */

.stop-session, .capture-image {
    /* Add styles for these buttons */
}

/* Responsive design example */
@media (max-width: 768px) {
    .initials-circle {
        width: 30px;
        height: 30px;
        line-height: 30px;
    }
}
